package view

import (
	"github.com/edgarsilva/go-scaffold/internal/model"
)

templ UsersIndexPage(todos []model.Todo, count, pending int, filter string, layoutProps layoutProps) {
	@Layout(layoutProps) {
		@CmpBtnBack("Back to home", "/")
		<section class="mt-16">
			// @TodoCreateForm()
			// @TodosList(todos)
			// @TodosFooter(count, pending, filter)
		</section>
	}
}
